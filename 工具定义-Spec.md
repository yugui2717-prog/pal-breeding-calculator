# 帕鲁配种计算器 · 工具定义（v1.1）

**定稿日期**：2026-08-10（t11 定义 + 问题3补充确认）
**用途**：阶段 3「做出第一个真工具」的施工图纸

## ① 一句话定义
一个给幻兽帕鲁玩家用的**配种查询工具**，在浏览器里用，解决"配种时需要一个一个查攻略"的痛点。

## ② 解决的痛点
玩家配种时不知道"哪两只配出什么"或"某只帕鲁怎么配出来"，只能逐个查抖音/攻略，且攻略数据可能过时（内测数据）。本工具让玩家**选一下就知道答案**。

## ③ 功能清单
### 核心功能（双模式，分区显示）
- **模式一（正向）**：用户选择爸爸 + 妈妈 → 显示配出的后代帕鲁
- **模式二（反向）**：用户选择目标帕鲁 → 列出所有能配出它的父母组合

### 辅助功能
- **查询历史（本地保存）**：每次查询自动保存到用户浏览器本地（localStorage），关闭浏览器后再打开仍在，可翻看历史记录
- 帕鲁下拉选择（数据源：pals.csv，299 只）
- **留言板（v1.2）**：玩家留言，存后端 JSON 文件；**置顶功能**（开发者手动置顶，金色样式置顶显示，2026-08-21 用户需求）

## ④ 使用场景
打开网页 → 选择模式（正向/反向）→ 选择帕鲁 → 查看结果 → 记录自动保存 → 下次打开还能翻看

## ⑤ 验收标准
- 双模式查询结果正确（BreedPower 平均值规则：后代 = 最接近父母平均值的那只）
- 查询记录刷新后不丢失（localStorage）
- 手机电脑都能用
- 界面中文，美观，游戏风格

## ⑥ 数据与技术
- **数据源**：tylercamp/palcalc pals.csv（2026-08-02 更新，299 只帕鲁含 BreedPower）
- **存储**：localStorage（浏览器本地，无需登录，无需数据库）
- **技术**：HTML + CSS + JS（纯前端单页应用）

## 待核对事项
- 变体帕鲁（85 只，IsVariant=True）的配种规则已处理（变异配种 v1.1 已支持）
- ⚠️ **已决策（2026-08-15，用户决定）**："世界树帕鲁自交产出突变蛋孵出磐甲龙"（如暮尘蛾自交→磐甲龙）**不加入工具**——该规则在权威数据源（palcalc）中不存在，未经游戏实测验证；加入会误导用户。如未来经游戏实测确认，再单独评估。

## 变更记录
- v1.0（8/10）：定义双模式 + 数据源调研完成
- **v1.1（8/10）：补充"查询历史本地保存"需求（用户确认）**
